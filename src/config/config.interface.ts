export interface IConfig {
    apiUrl: string
    kcUrl: string
    kcRealm: string
    kcClient: string
}
export interface IBuildVersion {
    lastBuild: string
    frameworkVersion: string
    client: string
    clientVersion: string
}
